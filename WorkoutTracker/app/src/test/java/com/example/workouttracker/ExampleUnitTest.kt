package com.example.workouttracker

import org.junit.Test

import org.junit.Assert.*

// Confirm that local JVM unit tests are configured correctly
class ExampleUnitTest {
    @Test
    // Run a simple assertion without requiring an Android device
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
